import * as ReactDOM from 'react-dom';
import * as React from 'react';
import MainView from './ViewComponents/MainView';

ReactDOM.render(React.createElement(MainView), document.getElementById("root"));